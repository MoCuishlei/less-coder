# L1 每日工作日志（2026-04-15）

日期：2026-04-15  
阶段：L1（Java 主链路 + CLI/发布能力）  
记录人：Codex + Owner

## 今日目标

1. 完成可安装能力（pip + npm）。
2. 完成发布流水线配置并打通触发路径。
3. 完成 CLI 在非仓库目录可用的路径解析与热处理能力。
4. 补齐文档结构与中文工作记录规范。

## 今日完成

- 完成 `lesscoder` 统一入口能力增强：
  - 支持 `run / trace / server / warmup`。
  - `server` 启动前自动 warmup。
  - warmup 支持显式执行与预编译。
- 协议层完成内部热处理机制：
  - adapter 新增 `system.warmup` action。
  - `LocalProtocolClient` 首次请求前自动触发 warmup。
- 完成发布流水线：
  - 新增 `.github/workflows/release.yml`（tag 触发、构建产物、GitHub Release、可选 PyPI/npm 发布）。
  - 修复 workflow 中 `if: secrets.*` 的表达式问题，改为 env guard。
- 完成文档整理：
  - README 去除“仅 MVP 文档”定位，统一为项目主说明。
  - 新增 `docs/README.md` 导航与 `docs/release_pipeline.md` 发布说明。
  - WORKLOG 顶层与 L1 增补中文索引/记录。

## 验证结果

- 命令验证：
  - `lesscoder --help` 正常。
  - `lesscoder warmup --skip-build` 正常返回结构化结果。
  - `lesscoder server` 支持非仓库目录启动（可自动发现 manifest）。
- 测试验证：
  - 目标测试通过：`tests/integration/test_lesscoder_entrypoint.py`、`test_orchestrator_protocol_client.py`。
  - 全量集成测试通过：`pytest -q tests/integration` -> `29 passed`。
- Rust 验证：
  - `cargo check --manifest-path engine/rust/alsp_adapter/Cargo.toml` 通过。

## 问题与处理

- 问题：GitHub Actions release workflow 报 `Unrecognized named-value: secrets`。
  - 处理：将 step 条件改为 `env.HAS_*` 判断，secrets 仅用于 env 注入与 action 参数。
- 问题：Windows 下 `alsp_adapter.exe` 偶发锁文件（os error 5）。
  - 处理：测试前清理残留进程；后续建议引入 release 二进制减少本地编译竞争。

## 风险与待办

- 当前 `server` 仍依赖本机 Rust/Cargo（对零配置用户不友好）。
- 发布步骤已具备，但 PyPI/npm 仍依赖仓库 secrets 完成最终发布。

## 明日建议

1. 增加“预编译 adapter 下载器”（替代 `cargo run`）以实现真正开箱即用。
2. 调整为参数驱动启动：不增加隐式 `lesscoder start` 一键流程，warmup 需显式传参。
3. 在 CI 增加发布 dry-run 校验（不发包，仅构建并校验 metadata）。
