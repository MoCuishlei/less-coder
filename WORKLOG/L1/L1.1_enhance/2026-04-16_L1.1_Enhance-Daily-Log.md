# L1.1 Enhance Daily Log (2026-04-16)

日期：2026-04-16  
阶段：L1.1 Enhance  
记录：Owner + Codex

## 今日目标

围绕场景主线完成 MVP 可用性加固：
- 随机目录启动不崩。
- warmup 依赖方法有明确门禁。
- warmup 后语义/图方法能调用。

## 今日完成

- 完成 `L1.1-P0-01`：CLI 增强项目根解析与错误提示。
  - 新增 `--project-root`。
  - 失败返回 `tried_paths` + `next_action`。

- 完成 `L1.1-P0-02`：Adapter 暴露 `system.health` 能力清单。
  - 方法级字段：`category`、`requires_warmup`、`available`、`description`。

- 完成 `L1.1-P0-03`：预热前门禁。
  - `symbol.lookup` / `symbol.lookup.static` / `symbol.resolve` / `graph.calls` 统一返回
    `COMMON_PRECONDITION_REQUIRED`。
  - 错误详情附 `system.warmup` 下一步动作示例。

- 完成 `L1.1-P0-04`：warmup 后可调用。
  - 新增 `graph.calls` 最小可用返回结构（v0）。
  - `system.health` 增加 `warmed_up` 状态。

- 完成 `L1.1-P0-05`：增量预热统计。
  - `system.warmup` 基于 Java 文件 `mtime` 做增量识别。
  - 返回 `changed_files` / `reindexed_files` / `duration_ms`。

- 完成 `L1.1-P1-01` / `P1-02` / `P1-03`：
  - `warmup/server` 必须显式传 `--project-root|--manifest-path`，不做隐式目录推断。
  - `server` 启动前检测端口占用并给出 `suggested_port`。
  - 新增 `lesscoder release-dry-run`，用于 tag 前完整 dry-run 验证。

- 测试更新：
  - Rust 单测新增：
    - `graph_calls_requires_warmup_before_use`
    - `graph_calls_after_warmup_returns_ok_shape`
    - `warmup_incremental_reports_only_changed_java_files`
  - 通过测试：
    - `cargo test --manifest-path engine/rust/alsp_adapter/Cargo.toml`
    - `pytest -q tests/integration/test_orchestrator_protocol_client.py tests/integration/test_real_chain_protocol.py tests/integration/test_lesscoder_entrypoint.py`
    - `pytest -q tests/integration`（全量通过）

## 当前风险

- `graph.calls` 目前仍是最小实现，尚未接真实调用图构建逻辑。
- `release-dry-run` 当前会产生构建产物（`dist/`、`*.tgz`），后续可补 `--clean` 选项做收尾清理。

## 下一步（明日）

1. 把 `graph.calls` 从最小占位实现升级为真实调用链分析。
2. 为增量预热补 Python 端到端集成测试（直接验证 `system.warmup` 增量统计）。
3. 视需要为 `release-dry-run` 增加 `--clean` 和 `--json-report`。
